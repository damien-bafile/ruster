# Inventory lines that are allowed to disappear across a merge.
#
# Every entry needs a reason. An unexplained entry here is how a real regression
# gets waved through, so the bar is: say what happened and why losing it is
# correct. Matched as whole lines against the removals the check reports.

# Phase 9 flips the default theme from "default" to "catppuccin-mocha". The
# `general.theme` key survives and the "default" theme is still in
# builtin_themes(); only the shipped default changed.
theme = "default"

# Renamed in crates/ruster-tui/src/lsp_state.rs when LSP clients became keyed by
# (language, project root) rather than language alone. Three tests replace it,
# including two that pin the new behaviour, so this is net +3.
ruster-tui lsp_state::tests::a_pending_action_is_keyed_by_language_as_well_as_id: test

# Never a real command. It appeared only as a dashboard Quick Actions string in
# widgets/mod.rs and the raylib backend — `parse_cmdline` has never had a branch
# for it, so the dashboard was telling new users to type something that did not
# exist. Replaced by :Files, which the parser does accept. Phase 9 adds
# tests/advertised_commands_exist.rs to stop it happening again.
:FuzzySearch
